// This file was auto-generated by Fern from our API Definition.

/**
 * @example
 *     {
 *         list_id: "list_id",
 *         subscriber_hash: "subscriber_hash",
 *         email_address: "email_address"
 *     }
 */
export interface UpsertMemberListsRequest {
    /** The unique ID for the list. */
    list_id: string;
    /** The MD5 hash of the lowercase version of the list member's email address. This endpoint also accepts a list member's email address or contact_id. */
    subscriber_hash: string;
    /** If skip_merge_validation is true, member data will be accepted without merge field values, even if the merge field is usually required. This defaults to false. */
    skip_merge_validation?: boolean;
    /** Email address for a subscriber. This value is required only if the email address is not already present on the list. */
    email_address: string;
    /** Type of email this member asked to get ('html' or 'text'). */
    email_type?: string;
    /** The key of this object's properties is the ID of the interest in question. */
    interests?: Record<string, boolean>;
    /** The IP address the subscriber used to confirm their opt-in status. */
    ip_opt?: string;
    /** IP address the subscriber signed up from. */
    ip_signup?: string;
    /** If set/detected, the [subscriber's language](https://mailchimp.com/help/view-and-edit-contact-languages/). */
    language?: string;
    /** Subscriber location information. */
    location?: UpsertMemberListsRequest.Location;
    /** The marketing permissions for the subscriber. */
    marketing_permissions?: UpsertMemberListsRequest.MarketingPermissions.Item[];
    /** A dictionary of merge fields where the keys are the merge tags. See the [Merge Fields documentation](https://mailchimp.com/developer/marketing/docs/merge-fields/#structure) for more about the structure. */
    merge_fields?: Record<string, UpsertMemberListsRequest.MergeFields.Value>;
    /** Subscriber's current status. */
    status?: UpsertMemberListsRequest.Status;
    /** Subscriber's status. This value is required only if the email address is not already present on the list. */
    status_if_new?: UpsertMemberListsRequest.StatusIfNew;
    /** The tags that are associated with a member. */
    tags?: string[];
    timestamp_opt?: UpsertMemberListsRequest.TimestampOpt;
    timestamp_signup?: UpsertMemberListsRequest.TimestampSignup;
    /** [VIP status](https://mailchimp.com/help/designate-and-send-to-vip-contacts/) for subscriber. */
    vip?: boolean;
}

export namespace UpsertMemberListsRequest {
    /**
     * Subscriber location information.
     */
    export interface Location {
        latitude?: Location.Latitude | undefined;
        longitude?: Location.Longitude | undefined;
    }

    export namespace Location {
        export type Latitude = number | string;
        export type Longitude = number | string;
    }

    export type MarketingPermissions = MarketingPermissions.Item[];

    export namespace MarketingPermissions {
        /**
         * A single marketing permission a subscriber has either opted-in to or opted-out of.
         */
        export interface Item {
            /** If the subscriber has opted-in to the marketing permission. */
            enabled?: boolean | undefined;
            /** The id for the marketing permission on the list */
            marketing_permission_id?: string | undefined;
        }
    }

    export namespace MergeFields {
        /**
         * This object's keys are merge tags (like FNAME). It's values are the values to be added to the merge field.
         */
        export type Value =
            | {
                  addr1: string;
                  addr2?: string | undefined;
                  city: string;
                  state: string;
                  zip: string;
                  country?: string | undefined;
              }
            | string
            | number;
    }

    /** Subscriber's current status. */
    export const Status = {
        Subscribed: "subscribed",
        Unsubscribed: "unsubscribed",
        Cleaned: "cleaned",
        Pending: "pending",
        Transactional: "transactional",
    } as const;
    export type Status = (typeof Status)[keyof typeof Status];
    /** Subscriber's status. This value is required only if the email address is not already present on the list. */
    export const StatusIfNew = {
        Subscribed: "subscribed",
        Unsubscribed: "unsubscribed",
        Cleaned: "cleaned",
        Pending: "pending",
        Transactional: "transactional",
    } as const;
    export type StatusIfNew = (typeof StatusIfNew)[keyof typeof StatusIfNew];
    export type TimestampOpt = string | "";
    export type TimestampSignup = string | "";
}
