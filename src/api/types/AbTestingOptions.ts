// This file was auto-generated by Fern from our API Definition.

/**
 * [A/B Testing](https://mailchimp.com/help/about-ab-tests/) options for a campaign.
 */
export interface AbTestingOptions {
    /** For campaigns split on 'From Name', the name for Group A. */
    from_name_a?: string | undefined;
    /** For campaigns split on 'From Name', the name for Group B. */
    from_name_b?: string | undefined;
    /** How we should evaluate a winner. Based on 'opens', 'clicks', or 'manual'. */
    pick_winner?: AbTestingOptions.PickWinner | undefined;
    /** For campaigns split on 'From Name', the reply-to address for Group A. */
    reply_email_a?: string | undefined;
    /** For campaigns split on 'From Name', the reply-to address for Group B. */
    reply_email_b?: string | undefined;
    /** The send time for Group A. */
    send_time_a?: string | undefined;
    /** The send time for Group B. */
    send_time_b?: string | undefined;
    /** The send time for the winning version. */
    send_time_winner?: string | undefined;
    /** The size of the split groups. Campaigns split based on 'schedule' are forced to have a 50/50 split. Valid split integers are between 1-50. */
    split_size?: number | undefined;
    /** The type of AB split to run. */
    split_test?: AbTestingOptions.SplitTest | undefined;
    /** For campaigns split on 'Subject Line', the subject line for Group A. */
    subject_a?: string | undefined;
    /** For campaigns split on 'Subject Line', the subject line for Group B. */
    subject_b?: string | undefined;
    /** The amount of time to wait before picking a winner. This cannot be changed after a campaign is sent. */
    wait_time?: number | undefined;
    /** How unit of time for measuring the winner ('hours' or 'days'). This cannot be changed after a campaign is sent. */
    wait_units?: AbTestingOptions.WaitUnits | undefined;
}

export namespace AbTestingOptions {
    /** How we should evaluate a winner. Based on 'opens', 'clicks', or 'manual'. */
    export const PickWinner = {
        Opens: "opens",
        Clicks: "clicks",
        Manual: "manual",
    } as const;
    export type PickWinner = (typeof PickWinner)[keyof typeof PickWinner];
    /** The type of AB split to run. */
    export const SplitTest = {
        Subject: "subject",
        FromName: "from_name",
        Schedule: "schedule",
    } as const;
    export type SplitTest = (typeof SplitTest)[keyof typeof SplitTest];
    /** How unit of time for measuring the winner ('hours' or 'days'). This cannot be changed after a campaign is sent. */
    export const WaitUnits = {
        Hours: "hours",
        Days: "days",
    } as const;
    export type WaitUnits = (typeof WaitUnits)[keyof typeof WaitUnits];
}
